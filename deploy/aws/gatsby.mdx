---
title: "How to deploy Gatsby to AWS"
sidebarTitle: "Gatsby"
description: "Deploy a Gatsby static site to your AWS account with S3 and CloudFront using Ravion, including build pipelines, custom domains, HTTPS, and CDN caching."
keywords: ["Gatsby AWS deployment", "deploy Gatsby to AWS", "Gatsby hosting", "S3", "CloudFront"]
"og:image": "https://www.ravion.com/og/docs/deploy/aws/gatsby.png"
"twitter:image": "https://www.ravion.com/og/docs/deploy/aws/gatsby.png"
---
import DeployPrerequisites from "/snippets/deploy/prerequisites.mdx";
import DeployNextSteps from "/snippets/deploy/next-steps.mdx";

Deploy Gatsby's generated `public` directory to S3 and serve it through CloudFront in your AWS account.

<DeployPrerequisites />

<Prompt description="Set up this Gatsby app for Ravion static hosting." icon="sparkles" actions={["copy", "cursor"]}>
  1. Before any other action, read and follow [https://www.ravion.com/docs/start-here#agent-workflow](https://www.ravion.com/docs/start-here#agent-workflow) as the mandatory agent workflow.
  2. Use https://www.ravion.com/docs/deploy/aws/gatsby as the framework-specific source of truth.
  3. Confirm this repository contains Gatsby and locate the app root in a monorepo.
  4. Inspect `package.json` and `gatsby-config.*` to detect the real build command and generated `public` directory, including any path prefix.
  5. Generate `ravion.yaml` with `rvn-aws-static`, filesystem routing, and the repository's real AWS account, region, branch, and slug.
  6. Ask the user for missing AWS account IDs, repository slugs, project IDs, or output paths before proceeding; do not guess.
</Prompt>

## Configure the deployment

This example uses [Railpack](https://railpack.com) for the build. Use `build_source: dockerfile` instead when you need a custom Dockerfile.

```yaml ravion.yaml
project:
  givenId: gatsby-site
  name: Gatsby site
environments:
  - givenId: production
    name: Production
    moduleInstances:
      - givenId: static-site
        name: Gatsby site
        type: rvn-aws-static
        version: 1.0.0
        input:
          aws_account_id: ravion-prod
          aws_region: us-east-1
          name: gatsby-site-production
          routing: filesystem
          build_source: railpack
          source_repo: my-org/my-gatsby-site
          output_directory: public
          railpack_build_cmd: npm run build
```

Gatsby runs `npm run build` and writes the production files to `public`. Add public build-time values to `build_environment_variables`.

## Apply and connect your domain

```bash
ravion project create --given-id gatsby-site --name "Gatsby site"
ravion project config apply gatsby-site --file ravion.yaml --dry-run
ravion project config apply gatsby-site --file ravion.yaml
```

Complete [custom domains](/guides/custom-domains) to configure DNS and TLS.

## Next steps

<DeployNextSteps />
