---
title: "How to deploy Docusaurus to AWS"
sidebarTitle: "Docusaurus"
description: "Deploy a Docusaurus documentation site to your AWS account with S3 and CloudFront using Ravion, including build pipelines, HTTPS, and custom domains."
keywords: ["Docusaurus AWS deployment", "deploy Docusaurus to AWS", "Docusaurus hosting", "S3", "CloudFront"]
"og:image": "https://www.ravion.com/og/docs/deploy/aws/docusaurus.png"
"twitter:image": "https://www.ravion.com/og/docs/deploy/aws/docusaurus.png"
---
import DeployPrerequisites from "/snippets/deploy/prerequisites.mdx";
import DeployNextSteps from "/snippets/deploy/next-steps.mdx";

Deploy the Docusaurus `build` directory to S3 and serve it globally through CloudFront.

<DeployPrerequisites />

<Prompt description="Set up this Docusaurus app for Ravion static hosting." icon="sparkles" actions={["copy", "cursor"]}>
  1. Before any other action, read and follow [https://www.ravion.com/docs/start-here#agent-workflow](https://www.ravion.com/docs/start-here#agent-workflow) as the mandatory agent workflow.
  2. Use https://www.ravion.com/docs/deploy/aws/docusaurus as the framework-specific source of truth.
  3. Confirm this repository contains Docusaurus and locate the app root in a monorepo.
  4. Inspect `package.json` and `docusaurus.config.*` to detect the actual build command, `build` output, `url`, and `baseUrl`.
  5. Generate `ravion.yaml` with `rvn-aws-static`, filesystem routing, and the repository's real AWS, branch, slug, and output values.
  6. Ask the user for missing AWS account IDs, repository slugs, project IDs, or base paths before proceeding; do not guess.
</Prompt>

## Configure the deployment

Ravion provisions the resources in your AWS account.

This example uses [Railpack](https://railpack.com) for the build. Use `build_source: dockerfile` instead when you need a custom Dockerfile.

```yaml ravion.yaml
project:
  givenId: docusaurus-site
  name: Docusaurus site
environments:
  - givenId: production
    name: Production
    moduleInstances:
      - givenId: static-site
        name: Docusaurus site
        type: rvn-aws-static
        version: 1.0.0
        input:
          aws_account_id: ravion-prod
          aws_region: us-east-1
          name: docusaurus-site-production
          routing: filesystem
          build_source: railpack
          source_repo: my-org/my-docusaurus-site
          output_directory: build
          railpack_build_cmd: npm run build
```

Docusaurus uses `npm run build` and writes the production site to `build`. Put public build-time settings in `build_environment_variables`.

## Apply and connect your domain

```bash
ravion project create --given-id docusaurus-site --name "Docusaurus site"
ravion project config apply docusaurus-site --file ravion.yaml --dry-run
ravion project config apply docusaurus-site --file ravion.yaml
```

Point your domain at the CloudFront hostname and complete [custom domains](/guides/custom-domains).

## Next steps

<DeployNextSteps />
