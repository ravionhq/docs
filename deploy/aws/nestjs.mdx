---
title: "How to deploy NestJS to AWS"
sidebarTitle: "NestJS"
description: "Deploy a NestJS application on ECS Fargate behind an Application Load Balancer in your AWS account with Ravion, including autoscaling, HTTPS, and CI/CD."
keywords: ["NestJS AWS deployment", "deploy NestJS to AWS", "NestJS dist main.js", "ECS Fargate", "Application Load Balancer"]
"og:image": "https://www.ravion.com/og/docs/deploy/aws/nestjs.png"
"twitter:image": "https://www.ravion.com/og/docs/deploy/aws/nestjs.png"
---
import DeployPrerequisites from "/snippets/deploy/prerequisites.mdx";
import DeployNextSteps from "/snippets/deploy/next-steps.mdx";

Deploy a NestJS server on ECS Fargate behind an Application Load Balancer.

<DeployPrerequisites />

<Prompt description="Set up this NestJS app for Ravion ECS hosting." icon="sparkles" actions={["copy", "cursor"]}>
  1. Before any other action, read and follow [https://www.ravion.com/docs/start-here#agent-workflow](https://www.ravion.com/docs/start-here#agent-workflow) as the mandatory agent workflow.
  2. Use https://www.ravion.com/docs/deploy/aws/nestjs as the framework-specific source of truth.
  3. Confirm this repository contains NestJS and locate the app root in a monorepo.
  4. Inspect `package.json`, `nest-cli.json`, TypeScript config, and source bootstrap to detect the real build command, `dist` entrypoint, port, and health route; bind to `0.0.0.0` and `process.env.PORT`.
  5. Generate `ravion.yaml` with `rvn-aws-network` + `rvn-ecs-cluster` + `rvn-ecs-web`, using real AWS account, region, repository, branch, and runtime values.
  6. Ask the user for AWS account IDs, repository slugs, project IDs, entrypoints, or ports before proceeding; do not guess.
</Prompt>

## Configure the deployment

The Nest CLI production build writes JavaScript to `dist`, with the standard starter entry at `dist/main.js`.

This example uses [Railpack](https://railpack.com) for the build. Use `build_source: dockerfile` instead when you need a custom Dockerfile.

```yaml ravion.yaml
project:
  givenId: nestjs-app
  name: NestJS app
environments:
  - givenId: production
    name: Production
    moduleInstances:
      - givenId: network
        name: Network
        type: rvn-aws-network
        version: 1.0.0
        input:
          aws_account_id: ravion-prod
          aws_region: us-east-1
          name: nestjs-production
      - givenId: cluster
        name: ECS cluster
        type: rvn-ecs-cluster
        version: 1.0.0
        input:
          network:
            moduleGivenIdRef: network
          name: nestjs-production
      - givenId: web
        name: NestJS web service
        type: rvn-ecs-web
        version: 1.0.0
        input:
          cluster:
            moduleGivenIdRef: cluster
          name: nestjs-production-web
          build_source: railpack
          source_repo: my-org/my-nestjs-app
          railpack_build_cmd: npm run build
          railpack_start_cmd: node dist/main.js
          container_port: 3000
          fargate_size:
            vcpu: 0.5
            memory_gb: 1
```

Set the application to listen on `0.0.0.0` and use port `3000`, or read `PORT` from the environment. Add a health endpoint and change `health_check_path` when your app does not serve `/`.

## Apply and connect your domain

```bash
ravion project create --given-id nestjs-app --name "NestJS app"
ravion project config apply nestjs-app --file ravion.yaml --dry-run
ravion project config apply nestjs-app --file ravion.yaml
```

Follow [custom domains](/guides/custom-domains) to connect your domain to the Application Load Balancer.

## Next steps

<DeployNextSteps />
