---
title: "How to deploy Bun to AWS"
sidebarTitle: "Bun"
description: "Deploy a Bun HTTP server on ECS Fargate behind an Application Load Balancer in your AWS account with Ravion, including autoscaling, HTTPS, and CI/CD."
keywords: ["Bun AWS deployment", "deploy Bun to AWS", "Bun.serve", "ECS Fargate", "Application Load Balancer"]
"og:image": "https://www.ravion.com/og/docs/deploy/aws/bun.png"
"twitter:image": "https://www.ravion.com/og/docs/deploy/aws/bun.png"
---
import DeployPrerequisites from "/snippets/deploy/prerequisites.mdx";
import DeployNextSteps from "/snippets/deploy/next-steps.mdx";

Deploy a Bun HTTP server on ECS Fargate behind an Application Load Balancer.

<DeployPrerequisites />

<Prompt description="Set up this Bun app for Ravion ECS hosting." icon="sparkles" actions={["copy", "cursor"]}>
  1. Before any other action, read and follow [https://www.ravion.com/docs/start-here#agent-workflow](https://www.ravion.com/docs/start-here#agent-workflow) as the mandatory agent workflow.
  2. Use https://www.ravion.com/docs/deploy/aws/bun as the framework-specific source of truth.
  3. Confirm this repository contains Bun and locate the app root in a monorepo.
  4. Inspect `package.json`, `bun.lock`, source entrypoints, and Docker files to detect the real build/start command and port; bind `Bun.serve` to `0.0.0.0` and `process.env.PORT`.
  5. Generate `ravion.yaml` with `rvn-aws-network` + `rvn-ecs-cluster` + `rvn-ecs-web` using Railpack and real AWS and repository values. Ask the user for the Bun entrypoint if Railpack cannot determine it.
  6. Ask the user for AWS account IDs, repository slugs, project IDs, entrypoints, or ports before proceeding; do not guess.
</Prompt>

## Configure the deployment

Bun's official server API uses `Bun.serve` and honors `PORT` when you do not set a port explicitly.

[Railpack](https://railpack.com) detects Bun from the `packageManager` field, `engines.bun`, or a `bun.lock` or `bun.lockb` file. Otherwise it uses its Node.js framework, monorepo, or SPA detection. Set the production start command to your actual Bun entrypoint when it is not inferred from the repository. Ravion injects `PORT` automatically from `container_port`.

```yaml ravion.yaml
project:
  givenId: bun-app
  name: Bun app
environments:
  - givenId: production
    name: Production
    moduleInstances:
      - givenId: network
        name: Network
        type: rvn-aws-network
        version: 1.0.0
        input:
          aws_account_id: ravion-prod
          aws_region: us-east-1
          name: bun-production
      - givenId: cluster
        name: ECS cluster
        type: rvn-ecs-cluster
        version: 1.0.0
        input:
          network:
            moduleGivenIdRef: network
          name: bun-production
      - givenId: web
        name: Bun web service
        type: rvn-ecs-web
        version: 1.0.0
        input:
          cluster:
            moduleGivenIdRef: cluster
          name: bun-production-web
          build_source: railpack
          source_repo: my-org/my-bun-app
          container_port: 3000
          fargate_size:
            vcpu: 0.5
            memory_gb: 1
```

Set `port: Number(process.env.PORT) || 3000` and `hostname: "0.0.0.0"` in `Bun.serve`, and set the runtime start command to your actual Bun entrypoint.

## Apply and connect your domain

```bash
ravion project create --given-id bun-app --name "Bun app"
ravion project config apply bun-app --file ravion.yaml --dry-run
ravion project config apply bun-app --file ravion.yaml
```

Follow [custom domains](/guides/custom-domains) to connect your domain to the Application Load Balancer.

## Next steps

<DeployNextSteps />
