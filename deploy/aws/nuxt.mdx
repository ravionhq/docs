---
title: "How to deploy Nuxt to AWS"
sidebarTitle: "Nuxt"
description: "Deploy a Nuxt server-rendered app on ECS Fargate behind an Application Load Balancer in your AWS account with Ravion, including autoscaling, HTTPS, and CI/CD."
keywords: ["Nuxt AWS deployment", "deploy Nuxt to AWS", "Nuxt Nitro", "ECS Fargate", "ALB"]
"og:image": "https://www.ravion.com/og/docs/deploy/aws/nuxt.png"
"twitter:image": "https://www.ravion.com/og/docs/deploy/aws/nuxt.png"
---
import DeployPrerequisites from "/snippets/deploy/prerequisites.mdx";
import DeployNextSteps from "/snippets/deploy/next-steps.mdx";

Nuxt's Node server runs on ECS Fargate behind an Application Load Balancer.

<DeployPrerequisites />

<Prompt description="Set up this Nuxt Nitro app for Ravion ECS hosting." icon="sparkles" actions={["copy", "cursor"]}>
  1. Before any other action, read and follow [https://www.ravion.com/docs/start-here#agent-workflow](https://www.ravion.com/docs/start-here#agent-workflow) as the mandatory agent workflow.
  2. Use https://www.ravion.com/docs/deploy/aws/nuxt as the framework-specific source of truth.
  3. Confirm this repository contains Nuxt and locate the app root in a monorepo.
  4. Inspect `package.json`, `nuxt.config.*`, Nitro settings, and runtime config to detect the real build/start commands, `.output` entrypoint, port, and health route.
  5. Generate `ravion.yaml` with `rvn-aws-network` + `rvn-ecs-cluster` + `rvn-ecs-web`, binding Nitro to `0.0.0.0` and `process.env.PORT`, with real AWS and repository values.
  6. Ask the user for AWS account IDs, repository slugs, project IDs, Nitro output, or ports before proceeding; do not guess.
</Prompt>

## Configure the deployment

Ravion creates the VPC, ECS cluster, service, and load balancer in your AWS account.

This example uses [Railpack](https://railpack.com) for the build. Use `build_source: dockerfile` instead when you need a custom Dockerfile.

Nuxt builds with `nuxt build` and starts with `node .output/server/index.mjs`. The server listens on port 3000.

```yaml ravion.yaml
project:
  givenId: nuxt-app
  name: Nuxt app
environments:
  - givenId: production
    name: Production
    moduleInstances:
      - givenId: network
        name: Network
        type: rvn-aws-network
        version: 1.0.0
        input:
          aws_account_id: ravion-prod
          aws_region: us-east-1
          name: nuxt-production
      - givenId: cluster
        name: ECS cluster
        type: rvn-ecs-cluster
        version: 1.0.0
        input:
          network:
            moduleGivenIdRef: network
          name: nuxt-production
      - givenId: web
        name: Nuxt web service
        type: rvn-ecs-web
        version: 1.0.0
        input:
          cluster:
            moduleGivenIdRef: cluster
          name: nuxt-production-web
          build_source: railpack
          source_repo: my-org/my-nuxt-app
          railpack_build_cmd: npm run build
          railpack_start_cmd: node .output/server/index.mjs
          container_port: 3000
          fargate_size:
            vcpu: 0.5
            memory_gb: 1
```

Set runtime values in `environment_variables`. Nuxt exposes browser-safe values through `runtimeConfig.public`; keep secrets in `secrets`.

## Apply and connect your domain

```bash
ravion project create --given-id nuxt-app --name "Nuxt app"
ravion project config apply nuxt-app --file ravion.yaml --dry-run
ravion project config apply nuxt-app --file ravion.yaml
```

Complete [custom domains](/guides/custom-domains) for the ALB hostname and TLS.

## Next steps

<DeployNextSteps />
